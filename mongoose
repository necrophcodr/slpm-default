URL=https://github.com/valenok/mongoose
TYPE=git
GITARGS="-b 5.1"
DESC="Mongoose web server"
PKGINSTALL=_mongooseinstall

_mongooseinstall() {
	eval $@
		make "${MAKEFLAGS}" -C build/ mongoose-lua-sqlite
		install build/mongoose-lua-sqlite "${PREFIX}${DESTDIR}/bin/mongoose"
}